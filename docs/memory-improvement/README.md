# Memory Improvement

Visible GitHub label for the long-term memory cleanup effort.

- `main` = stable baseline
- `Update` = long-term working branch for future phases/PRs
- `TASK ID 0011` = umbrella task for the cleanup

This repository path exists only so the cleanup remains searchable and reviewable on GitHub.
